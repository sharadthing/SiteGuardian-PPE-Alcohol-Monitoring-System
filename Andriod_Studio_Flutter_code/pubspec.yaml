name: live_camera_26_march
description: "A Flutter application for PPE detection with live camera feed."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_svg: ^2.0.7
  http: any
  webview_flutter: ^4.10.0
  webview_flutter_wkwebview: ^3.18.5
  shared_preferences: ^2.5.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0  # Updated to latest

flutter:
  uses-material-design: true

