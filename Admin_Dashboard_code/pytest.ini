# pytest.ini
[pytest]
# Where pytest should look for your test files
testpaths = tests

# Which file name patterns to collect
python_files = test_*.py

# Prepend the project root to PYTHONPATH so 'import routes' works
python_paths = .
